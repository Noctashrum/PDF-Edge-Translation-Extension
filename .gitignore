# ═══════════════════════════════════════════════════════════════
# PDF 划词翻译 · Git 忽略规则
#
# 原则：第三方代码、以及「一条命令就能重新生成」的产物都不入库，
#       仓库里只保留自己写的源码、文档和测试。
#       克隆之后跑一次 `npm run build`，就能得到完整可加载的扩展。
# ═══════════════════════════════════════════════════════════════

# ── 依赖 ───────────────────────────────────────────────────────
# 本项目零运行时依赖（无 import 第三方包），正常不会出现这个目录
node_modules/

# ── `npm run build` 生成进扩展目录的资源 ───────────────────────
# pdfjs-dist 的运行时资源（约 5.7 MB 第三方代码，来自 npm 包 pdfjs-dist）
extension/vendor/
# 扩展图标，由 tools/make-icons.mjs 现场绘制
extension/icons/

# ── 构建 / 测试产物 ────────────────────────────────────────────
# `npm run pack` 产出的分发 zip
dist/
# 端到端测试保存的截图（docs/ 里那几张是给 README 用的，要入库）
tests/artifacts/
# `node tools/pack.mjs --crx` 产出的 crx 与私钥（私钥绝不能入库）
*.crx
*.pem

# ── 下载缓存 ───────────────────────────────────────────────────
# pdfjs-dist 的 npm pack 缓存，`npm run vendor` 会按需重建
.tmp/

# ── 编辑器 / 系统杂项 ──────────────────────────────────────────
*.log
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
